'use client'
import { useState, useEffect, useRef } from 'react'
const CATS=[{id:'ids',label:'IDs & Passports',icon:'🪪'},{id:'insurance',label:'Insurance',icon:'🛡️'},{id:'medical',label:'Medical',icon:'🏥'},{id:'financial',label:'Financial',icon:'💰'},{id:'property',label:'Property & Vehicles',icon:'🏠'},{id:'kids',label:'Kids',icon:'👶'},{id:'pets',label:'Pets',icon:'🐾'},{id:'legal',label:'Legal & Estate',icon:'⚖️'},{id:'other',label:'Other',icon:'📁'}]
type Doc={id:string;name:string;category:string;notes:string;expires?:string;createdAt:string}
type ChatMsg={role:'user'|'assistant';content:string}
const SK='familyvault_docs',PK='familyvault_pin'
function dU(d:string){return Math.ceil((new Date(d).getTime()-new Date().getTime())/(864e5))}
export default function App(){
  const[pin,setPin]=useState(''),[savedPin,setSavedPin]=useState<string|null>(null),[pinInput,setPinInput]=useState(''),[pinError,setPinError]=useState(''),[unlocked,setUnlocked]=useState(false),[settingPin,setSettingPin]=useState(false),[confirmPin,setConfirmPin]=useState('')
  const[docs,setDocs]=useState<Doc[]>([]),[view,setView]=useState<'vault'|'add'|'doc'|'chat'|'settings'>('vault'),[selCat,setSelCat]=useState<string|null>(null),[selDoc,setSelDoc]=useState<Doc|null>(null),[search,setSearch]=useState('')
  const[form,setForm]=useState({name:'',category:'',notes:'',expires:''}),[editMode,setEditMode]=useState(false)
  const[msgs,setMsgs]=useState<ChatMsg[]>([]),[chatIn,setChatIn]=useState(''),[chatLoad,setChatLoad]=useState(false),chatRef=useRef<HTMLDivElement>(null)
  useEffect(()=>{const sp=localStorage.getItem(PK);setSavedPin(sp);if(!sp){setSettingPin(true);setUnlocked(false)};const st=localStorage.getItem(SK);if(st)setDocs(JSON.parse(st))},[])
  useEffect(()=>{chatRef.current?.scrollIntoView({behavior:'smooth'})},[msgs])
  const save=(d:Doc[])=>{setDocs(d);localStorage.setItem(SK,JSON.stringify(d))}
  const setP=()=>{if(pin.length<4){setPinError('PIN must be at least 4 digits');return};if(pin!==confirmPin){setPinError('PINs do not match');return};localStorage.setItem(PK,pin);setSavedPin(pin);setSettingPin(false);setUnlocked(true);setPinError('')}
  const unlock=()=>{if(pinInput===savedPin){setUnlocked(true);setPinError('');setPinInput('')}else{setPinError('Incorrect PIN');setPinInput('')}}
  const addDoc=()=>{if(!form.name||!form.category)return;if(editMode&&selDoc){save(docs.map(d=>d.id===selDoc.id?{...d,...form}:d));setSelDoc({...selDoc,...form})}else{save([...docs,{...form,id:Date.now().toString(),createdAt:new Date().toISOString()}])};setForm({name:'',category:'',notes:'',expires:''});setEditMode(false);setView(editMode?'doc':'vault')}
  const delDoc=(id:string)=>{save(docs.filter(d=>d.id!==id));setView('vault')}
  const sendChat=async()=>{if(!chatIn.trim()||chatLoad)return;const um:ChatMsg={role:'user',content:chatIn},nm=[...msgs,um];setMsgs(nm);setChatIn('');setChatLoad(true);try{const r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({messages:nm,docSummary:docs.map(d=>d.name+' ('+d.category+')').join(', ')})});const d=await r.json();setMsgs([...nm,{role:'assistant',content:d.reply||'Error'}])}catch{setMsgs([...nm,{role:'assistant',content:'Error connecting.'}])}finally{setChatLoad(false)}}
  const expiring=docs.filter(d=>d.expires&&dU(d.expires)<=90&&dU(d.expires)>=0)
  const filtered=docs.filter(d=>(!selCat||d.category===selCat)&&(!search||d.name.toLowerCase().includes(search.toLowerCase())||d.notes.toLowerCase().includes(search.toLowerCase())))
  if(!unlocked)return(<main className="min-h-screen bg-slate-950 flex items-center justify-center px-4"><div className="w-full max-w-sm"><div className="text-center mb-8"><div className="text-5xl mb-4">🔒</div><h1 className="text-2xl font-bold text-white">FamilyVault</h1><p className="text-slate-400 text-sm mt-1">{settingPin?'Create your PIN to get started':'Enter your PIN to unlock'}</p></div><div className="bg-slate-900 border border-slate-800 rounded-2xl p-6">{settingPin?(<><label className="text-slate-400 text-xs uppercase tracking-wider mb-2 block">Create PIN</label><input type="password" inputMode="numeric" value={pin} onChange={e=>setPin(e.target.value)} placeholder="••••" className="w-full bg-slate-800 border border-slate-700 rounded-xl px-4 py-3 text-white text-center text-2xl tracking-widest outline-none focus:border-blue-500 mb-4"/><label className="text-slate-400 text-xs uppercase tracking-wider mb-2 block">Confirm PIN</label><input type="password" inputMode="numeric" value={confirmPin} onChange={e=>setConfirmPin(e.target.value)} placeholder="••••" onKeyDown={e=>e.key==='Enter'&&setP()} className="w-full bg-slate-800 border border-slate-700 rounded-xl px-4 py-3 text-white text-center text-2xl tracking-widest outline-none focus:border-blue-500 mb-4"/>{pinError&&<p className="text-red-400 text-sm text-center mb-3">{pinError}</p>}<button onClick={setP} className="w-full bg-blue-600 hover:bg-blue-500 text-white font-bold py-3 rounded-xl transition-all">Create Vault</button></>):(<><label className="text-slate-400 text-xs uppercase tracking-wider mb-2 block">PIN</label><input type="password" inputMode="numeric" value={pinInput} onChange={e=>setPinInput(e.target.value)} placeholder="••••" onKeyDown={e=>e.key==='Enter'&&unlock()} autoFocus className="w-full bg-slate-800 border border-slate-700 rounded-xl px-4 py-3 text-white text-center text-2xl tracking-widest outline-none focus:border-blue-500 mb-4"/>{pinError&&<p className="text-red-400 text-sm text-center mb-3">{pinError}</p>}<button onClick={unlock} className="w-full bg-blue-600 hover:bg-blue-500 text-white font-bold py-3 rounded-xl transition-all">Unlock</button></>)}</div></div></main>)
  if(view==='add')return(<main className="min-h-screen bg-slate-950 text-white"><div className="max-w-2xl mx-auto px-4 py-8"><div className="flex items-center gap-4 mb-8"><button onClick={()=>{setView(editMode?'doc':'vault');setEditMode(false);setForm({name:'',category:'',notes:'',expires:''})}} className="text-slate-400 hover:text-white">← Back</button><h1 className="text-xl font-bold">{editMode?'Edit Document':'Add Document'}</h1></div><div className="space-y-5"><div><label className="text-slate-400 text-xs uppercase tracking-wider mb-2 block">Document Name *</label><input value={form.name} onChange={e=>setForm(f=>({...f,name:e.target.value}))} placeholder="e.g. John's Passport" className="w-full bg-slate-900 border border-slate-700 focus:border-blue-500 rounded-xl px-4 py-3 outline-none transition-all"/></div><div><label className="text-slate-400 text-xs uppercase tracking-wider mb-2 block">Category *</label><div className="grid grid-cols-3 gap-2">{CATS.map(c=>(<button key={c.id} onClick={()=>setForm(f=>({...f,category:c.id}))} className={"flex items-center gap-2 px-3 py-2.5 rounded-xl border text-sm font-medium transition-all "+(form.category===c.id?'border-blue-500 bg-blue-500/10 text-blue-400':'border-slate-700 bg-slate-900 text-slate-300 hover:border-slate-500')}><span>{c.icon}</span><span className="truncate">{c.label}</span></button>))}</div></div><div><label className="text-slate-400 text-xs uppercase tracking-wider mb-2 block">Notes / Details</label><textarea value={form.notes} onChange={e=>setForm(f=>({...f,notes:e.target.value}))} placeholder="Policy number, location, account number..." rows={4} className="w-full bg-slate-900 border border-slate-700 focus:border-blue-500 rounded-xl px-4 py-3 outline-none transition-all resize-none"/></div><div><label className="text-slate-400 text-xs uppercase tracking-wider mb-2 block">Expiration Date (optional)</label><input type="date" value={form.expires} onChange={e=>setForm(f=>({...f,expires:e.target.value}))} className="w-full bg-slate-900 border border-slate-700 focus:border-blue-500 rounded-xl px-4 py-3 outline-none transition-all"/></div><button onClick={addDoc} disabled={!form.name||!form.category} className="w-full bg-blue-600 hover:bg-blue-500 disabled:opacity-40 text-white font-bold py-3.5 rounded-xl transition-all">{editMode?'Save Changes':'+ Add to Vault'}</button></div></div></main>)
  if(view==='doc'&&selDoc){const cat=CATS.find(c=>c.id===selDoc.category),days=selDoc.expires?dU(selDoc.expires):null;return(<main className="min-h-screen bg-slate-950 text-white"><div className="max-w-2xl mx-auto px-4 py-8"><button onClick={()=>setView('vault')} className="text-slate-400 hover:text-white mb-8 block">← Back</button><div className="bg-slate-900 border border-slate-800 rounded-2xl p-6"><div className="flex items-start justify-between mb-6"><div className="flex items-center gap-3"><span className="text-3xl">{cat?.icon}</span><div><h1 className="text-xl font-bold">{selDoc.name}</h1><p className="text-slate-400 text-sm">{cat?.label}</p></div></div><div className="flex gap-2"><button onClick={()=>{setForm({name:selDoc.name,category:selDoc.category,notes:selDoc.notes,expires:selDoc.expires||''});setEditMode(true);setView('add')}} className="text-blue-400 text-sm px-3 py-1.5 border border-blue-500/30 rounded-lg">Edit</button><button onClick={()=>{if(confirm('Delete this document?'))delDoc(selDoc.id)}} className="text-red-400 text-sm px-3 py-1.5 border border-red-500/30 rounded-lg">Delete</button></div></div>{selDoc.expires&&<div className={"mb-4 px-4 py-3 rounded-xl text-sm font-medium "+(days!==null&&days<=30?'bg-red-500/10 border border-red-500/30 text-red-400':days!==null&&days<=90?'bg-yellow-500/10 border border-yellow-500/30 text-yellow-400':'bg-slate-800 text-slate-400')}>📅 Expires {new Date(selDoc.expires).toLocaleDateString('en-US',{year:'numeric',month:'long',day:'numeric'})}{days!==null&&days>=0&&<span className="ml-2">({days} days away)</span>}{days!==null&&days<0&&<span className="ml-2 text-red-400 font-bold">(EXPIRED)</span>}</div>}{selDoc.notes&&<div className="bg-slate-800 rounded-xl p-4"><p className="text-slate-400 text-xs uppercase tracking-wider mb-2">Notes</p><p className="text-slate-200 whitespace-pre-wrap">{selDoc.notes}</p></div>}<p className="text-slate-600 text-xs mt-4">Added {new Date(selDoc.createdAt).toLocaleDateString()}</p></div></div></main>)}
  if(view==='chat')return(<main className="min-h-screen bg-slate-950 text-white flex flex-col"><div className="max-w-2xl mx-auto w-full px-4 py-6 flex flex-col flex-1"><div className="flex items-center gap-4 mb-6"><button onClick={()=>setView('vault')} className="text-slate-400 hover:text-white">← Back</button><div><h1 className="text-lg font-bold">Vault Assistant</h1><p className="text-slate-400 text-xs">Ask about your documents</p></div></div><div className="flex-1 space-y-4 mb-4" style={{maxHeight:'calc(100vh - 220px)',overflowY:'auto'}}>{msgs.length===0&&<div className="space-y-2"><p className="text-slate-500 text-sm text-center mb-4">Ask me anything about your family documents</p>{['What documents do I need to renew a passport?','What insurance docs should every family have?','Which of my documents are expiring soon?'].map(q=>(<button key={q} onClick={()=>setChatIn(q)} className="w-full text-left px-4 py-3 bg-slate-900 border border-slate-700 hover:border-slate-500 rounded-xl text-sm text-slate-300">{q}</button>))}</div>}{msgs.map((m,i)=>(<div key={i} className={"flex "+(m.role==='user'?'justify-end':'justify-start')}><div className={"max-w-[80%] px-4 py-3 rounded-2xl text-sm "+(m.role==='user'?'bg-blue-600 text-white':'bg-slate-800 text-slate-200')}>{m.content}</div></div>))}{chatLoad&&<div className="flex justify-start"><div className="bg-slate-800 px-4 py-3 rounded-2xl text-slate-400 text-sm">Thinking...</div></div>}<div ref={chatRef}/></div><div className="flex gap-3"><input value={chatIn} onChange={e=>setChatIn(e.target.value)} onKeyDown={e=>e.key==='Enter'&&sendChat()} placeholder="Ask about your documents..." className="flex-1 bg-slate-900 border border-slate-700 focus:border-blue-500 rounded-xl px-4 py-3 outline-none text-sm"/><button onClick={sendChat} disabled={!chatIn.trim()||chatLoad} className="bg-blue-600 hover:bg-blue-500 disabled:opacity-40 px-5 py-3 rounded-xl font-medium text-sm">Send</button></div></div></main>)
  if(view==='settings')return(<main className="min-h-screen bg-slate-950 text-white"><div className="max-w-2xl mx-auto px-4 py-8"><div className="flex items-center gap-4 mb-8"><button onClick={()=>setView('vault')} className="text-slate-400 hover:text-white">← Back</button><h1 className="text-xl font-bold">Settings</h1></div><div className="space-y-4"><div className="bg-slate-900 border border-slate-800 rounded-2xl p-5"><h2 className="font-semibold mb-1">Change PIN</h2><input type="password" inputMode="numeric" value={pin} onChange={e=>setPin(e.target.value)} placeholder="New PIN" className="w-full bg-slate-800 border border-slate-700 rounded-xl px-4 py-3 outline-none mb-3 focus:border-blue-500"/><input type="password" inputMode="numeric" value={confirmPin} onChange={e=>setConfirmPin(e.target.value)} placeholder="Confirm new PIN" className="w-full bg-slate-800 border border-slate-700 rounded-xl px-4 py-3 outline-none mb-3 focus:border-blue-500"/>{pinError&&<p className="text-red-400 text-sm mb-3">{pinError}</p>}<button onClick={()=>{if(pin.length>=4&&pin===confirmPin){localStorage.setItem(PK,pin);setSavedPin(pin);setPin('');setConfirmPin('');setPinError('PIN updated!')}else{setPinError('PINs must match and be 4+ digits')}}} className="bg-blue-600 hover:bg-blue-500 px-5 py-2.5 rounded-xl text-sm font-medium">Update PIN</button></div><div className="bg-slate-900 border border-slate-800 rounded-2xl p-5"><h2 className="font-semibold mb-1">Vault Stats</h2><p className="text-slate-400 text-sm">{docs.length} documents stored across {new Set(docs.map(d=>d.category)).size} categories</p></div><div className="bg-slate-900 border border-red-900/50 rounded-2xl p-5"><h2 className="font-semibold text-red-400 mb-1">Clear All Data</h2><button onClick={()=>{if(confirm('Delete ALL documents?')){save([]);localStorage.removeItem(PK);setUnlocked(false);setSavedPin(null);setSettingPin(true)}}} className="bg-red-600/20 border border-red-600/30 text-red-400 px-5 py-2.5 rounded-xl text-sm font-medium">Clear Everything</button></div></div></div></main>)
  return(<main className="min-h-screen bg-slate-950 text-white"><div className="max-w-2xl mx-auto px-4 py-8"><div className="flex items-center justify-between mb-8"><div className="flex items-center gap-3"><span className="text-2xl">🔒</span><div><h1 className="text-xl font-bold">FamilyVault</h1><p className="text-slate-400 text-xs">{docs.length} documents stored</p></div></div><div className="flex gap-2"><button onClick={()=>setView('chat')} className="p-2.5 bg-slate-800 hover:bg-slate-700 rounded-xl" title="AI Assistant">🤖</button><button onClick={()=>setView('settings')} className="p-2.5 bg-slate-800 hover:bg-slate-700 rounded-xl" title="Settings">⚙️</button><button onClick={()=>setUnlocked(false)} className="p-2.5 bg-slate-800 hover:bg-slate-700 rounded-xl" title="Lock">🔒</button></div></div>{expiring.length>0&&<div className="mb-6 bg-yellow-500/10 border border-yellow-500/30 rounded-xl p-4"><p className="text-yellow-400 font-semibold text-sm mb-2">⚠️ Expiring Soon</p>{expiring.map(d=>(<button key={d.id} onClick={()=>{setSelDoc(d);setView('doc')}} className="block text-yellow-300/80 text-sm hover:text-yellow-300">{d.name} — {dU(d.expires!)} days left</button>))}</div>}<input value={search} onChange={e=>setSearch(e.target.value)} placeholder="🔍 Search documents..." className="w-full bg-slate-900 border border-slate-700 focus:border-blue-500 rounded-xl px-4 py-3 outline-none mb-6 text-sm"/><div className="flex gap-2 overflow-x-auto pb-2 mb-6"><button onClick={()=>setSelCat(null)} className={"shrink-0 px-4 py-2 rounded-xl text-sm font-medium "+(!selCat?'bg-blue-600 text-white':'bg-slate-800 text-slate-400 hover:text-white')}>All</button>{CATS.map(c=>(<button key={c.id} onClick={()=>setSelCat(selCat===c.id?null:c.id)} className={"shrink-0 flex items-center gap-1.5 px-4 py-2 rounded-xl text-sm font-medium "+(selCat===c.id?'bg-blue-600 text-white':'bg-slate-800 text-slate-400 hover:text-white')}>{c.icon} {c.label}</button>))}</div>{filtered.length===0?(<div className="text-center py-20"><p className="text-5xl mb-4">📂</p><p className="text-slate-400 text-lg font-medium mb-2">{docs.length===0?'Your vault is empty':'No documents found'}</p><p className="text-slate-500 text-sm">{docs.length===0?'Add your first important document':'Try a different search or category'}</p></div>):(<div className="space-y-3">{filtered.map(doc=>{const cat=CATS.find(c=>c.id===doc.category),days=doc.expires?dU(doc.expires):null;return(<button key={doc.id} onClick={()=>{setSelDoc(doc);setView('doc')}} className="w-full flex items-center gap-4 bg-slate-900 hover:bg-slate-800 border border-slate-800 hover:border-slate-600 rounded-xl p-4 transition-all text-left"><span className="text-2xl">{cat?.icon}</span><div className="flex-1 min-w-0"><p className="font-medium truncate">{doc.name}</p><p className="text-slate-400 text-sm">{cat?.label}</p></div>{days!==null&&<span className={"text-xs font-medium px-2.5 py-1 rounded-lg shrink-0 "+(days<0?'bg-red-500/20 text-red-400':days<=30?'bg-red-500/20 text-red-400':days<=90?'bg-yellow-500/20 text-yellow-400':'bg-slate-800 text-slate-500')}>{days<0?'Expired':days+'d'}</span>}<span className="text-slate-600">›</span></button>)})}</div>)}<button onClick={()=>{setForm({name:'',category:'',notes:'',expires:''});setEditMode(false);setView('add')}} className="fixed bottom-6 right-6 bg-blue-600 hover:bg-blue-500 shadow-lg shadow-blue-500/25 w-14 h-14 rounded-full text-2xl flex items-center justify-center transition-all">+</button></div></main>)
}